# ──── 敏感配置（请复制为 .env.local 并填写实际值）────
#
# 此文件不会被加载，仅作为模板参考。
# 实际 API Key 通过 Web 控制台配置，保存在 data/providers.json 中。
#
# 如果你想通过环境变量覆盖默认配置，可在 .env.local 中设置：
# SUPERUSERS=["你的QQ号"]
# ALLOW_REMOTE_WEB=1   # 仅在你已配置额外鉴权时开启
